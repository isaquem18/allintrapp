// import { View } from 'react-native';
import * as S from './styles';

export default function Home() {
  return (
    <S.Container>
      {/* {Array(30)
        .fill('a')
        .map((_, index) => (
          <View
            key={`${index}-222`}
            style={{
              width: '100%',
              height: 100,
              backgroundColor: '#00f',
              marginBottom: 10,
            }}
          />
        ))} */}
    </S.Container>
  );
}
